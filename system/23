How to connect to new user with graphic session:
	1. Click top-right corner.
	2. Then click 'Power off / Log out'
	3. Then there should appear 'Switch User ...' button, click that.
	4. Now you can see list of your users, choose the new one. 
	5. Type the password.
	6. That's it now you are in!

In terminal you can use: `su [username]` for example `su emende`.

To connect with SSH Session:
	ssh user@ip address -p [port]
		- port only if needed.

	for example if you had user called test:
	`	`ssh test@localhost` would work.
